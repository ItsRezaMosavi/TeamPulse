name: Publish Packages

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

jobs:
  
  # ==========================================
  # Pack individual packages
  # ==========================================
  pack:
    name: Pack ${{ matrix.project }}
    runs-on: windows-latest
    
    strategy:
      matrix:
        project:
          - BuildingBlocks.Domain
          - BuildingBlocks.Application
          - BuildingBlocks.Infrastructure
          - BuildingBlocks.Persistence
          - BuildingBlocks.Results
          - BuildingBlocks.Specification
          - BuildingBlocks.Validation
          - BuildingBlocks.Pagination
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore BuildingBlocks.sln

      - name: Pack
        run: >
          dotnet pack
          src/${{ matrix.project }}/${{ matrix.project }}.csproj
          --configuration Release
          --no-restore
          --output ./packages/${{ matrix.project }}

      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.project }}
          path: ./packages/${{ matrix.project }}/*.nupkg
  
  
  # ==========================================
  # Pack complete package
  # ==========================================
  pack-all:
    name: Pack Reza.BuildingBlocks
    runs-on: windows-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore BuildingBlocks.sln

      - name: Pack complete package
        run: >
          dotnet pack
          src/BuildingBlocks/BuildingBlocks.csproj
          --configuration Release
          --no-restore
          --output ./packages/complete

      - name: Upload complete package
        uses: actions/upload-artifact@v4
        with:
          name: Reza.BuildingBlocks
          path: ./packages/complete/*.nupkg
  
  
  # ==========================================
  # Publish all packages
  # ==========================================
  publish:
    name: Publish to GitHub Packages and NuGet.org
    
    needs:
      - pack
      - pack-all
    
    runs-on: windows-latest
    
    permissions:
      contents: read
      packages: write
      id-token: write
    
    steps:
      - name: Download individual packages
        uses: actions/download-artifact@v4
        with:
          path: ./packages
          pattern: BuildingBlocks.*
          merge-multiple: true

      - name: Download complete package
        uses: actions/download-artifact@v4
        with:
          name: Reza.BuildingBlocks
          path: ./packages

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: List packages
        shell: pwsh
        run: |
          Write-Host "Packages:"
          Get-ChildItem -Path "./packages" -Recurse -Filter "*.nupkg"
      
      # ==========================================
      # Authenticate with NuGet.org
      # ==========================================
      - name: Login to NuGet.org
        uses: NuGet/login@v1
        id: nuget-login
        with:
          user: ${{ secrets.NUGET_USER }}
      
      # ==========================================
      # Publish packages
      # ==========================================
      - name: Publish packages
        shell: pwsh
        run: |
          $packages = Get-ChildItem -Path "./packages" -Recurse -Filter "*.nupkg"
          
          if ($packages.Count -eq 0) {
              Write-Error "No NuGet packages found."
              exit 1
          }
          
          foreach ($package in $packages) {
              Write-Host "Publishing $($package.Name) to GitHub Packages..."
          
              dotnet nuget push $package.FullName `
                --source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" `
                --api-key "${{ secrets.GITHUB_TOKEN }}" `
                --skip-duplicate
          
              Write-Host "Publishing $($package.Name) to NuGet.org..."
          
              dotnet nuget push $package.FullName `
                --source "https://api.nuget.org/v3/index.json" `
                --api-key "${{ steps.nuget-login.outputs.NUGET_API_KEY }}" `
                --skip-duplicate
          }